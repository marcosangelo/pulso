name: pulso
description: HUD gamer do Pulso — segundo monitor no celular.
publish_to: "none"
version: 0.3.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.12.0
  flutter_riverpod: ^3.4.2
  go_router: ^18.0.0
  google_fonts: ^8.2.1
  google_mlkit_barcode_scanning: ^0.16.1
  wakelock_plus: ^1.7.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
